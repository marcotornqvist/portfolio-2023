; The contact-form/email deps (@react-email/*, etc.) still declare a hard
; react@18 peer, which conflicts with React 19. Remove this once the contact
; form is deleted and those packages are removed.
legacy-peer-deps=true
